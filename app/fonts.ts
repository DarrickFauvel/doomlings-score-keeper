import { Lato, Anton } from "next/font/google"

export const lato = Lato({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
})

export const anton = Anton({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
})
